// Generated by CoffeeScript 2.4.1
(function() {
  'use strict';
  var $, $async, CND, FS, MIRAGE, PATH, PD, _$count, abspath, as_sql, assign, badge, debug, declare, echo, help, info, isa, jr, last_of, relpath, rpr, select, size_of, type_of, types, urge, validate, warn, whisper,
    modulo = function(a, b) { return (+a % (b = +b) + b) % b; };

  //###########################################################################################################
  CND = require('cnd');

  rpr = CND.rpr;

  badge = 'MKTS-MIRAGE/MAIN';

  debug = CND.get_logger('debug', badge);

  warn = CND.get_logger('warn', badge);

  info = CND.get_logger('info', badge);

  urge = CND.get_logger('urge', badge);

  help = CND.get_logger('help', badge);

  whisper = CND.get_logger('whisper', badge);

  echo = CND.echo.bind(CND);

  //...........................................................................................................
  FS = require('fs');

  PATH = require('path');

  PD = require('pipedreams');

  ({$, $async, select} = PD);

  ({assign, jr} = CND);

  this._drop_extension = function(path) {
    return path.slice(0, path.length - (PATH.extname(path)).length);
  };

  types = require('./types');

  //...........................................................................................................
  ({isa, validate, declare, size_of, last_of, type_of} = types);

  //...........................................................................................................
  ({assign, abspath, relpath} = require('./helpers'));

  //...........................................................................................................
  require('./exception-handler');

  //-----------------------------------------------------------------------------------------------------------
  as_sql = function(x) {
    var R;
    validate.text(x);
    R = x;
    R = R.replace(/'/g, "''");
    return `'${R}'`;
  };

  //-----------------------------------------------------------------------------------------------------------
  this.$as_sql = () => {
    var first, is_first_record, last, lnr;
    first = Symbol('first');
    last = Symbol('last');
    is_first_record = true;
    lnr = 0;
    return $({first, last}, (line, send) => {
      var comma;
      //.......................................................................................................
      /* TAINT consider to store SQL as `fragment`s in `mkts.icql` */
      if (line === first) {
        send("drop table if exists main;");
        send("create table main ( ");
        send("    vnr_txt   json,");
        // send "    vnr_txt  json unique,"
        send("    stamped   boolean default false,");
        send("    key       text default '^mktscript',");
        send("    value     text );");
        return send("insert into main ( vnr_txt, value ) values");
      //.......................................................................................................
      } else if (line === last) {
        return send(";");
      } else {
        // send "create unique index idx_main_lnr on main ( lnr );"
        //.......................................................................................................
        lnr += +1;
        comma = is_first_record ? '' : ',';
        is_first_record = false;
        return send(`${comma}( '[${lnr}]', ${as_sql(line)} )`);
      }
    });
    //.........................................................................................................
    return null;
  };

  //-----------------------------------------------------------------------------------------------------------
  this._$tee_without_filter = function(bystream) {
    /* Given a `bystream`, send a data down both the mainstream and the bystream. This allows e.g. to log all
    events to a file sink while continuing to process the same data in the mainline. **NB** that in
    contradistinction to `pull-tee`, you can only divert to a single by-stream with each call to `PS.$tee` */
    return (require('pull-tee'))(bystream);
  };

  //-----------------------------------------------------------------------------------------------------------
  this.$tee_compile_sql = (target_path_sql, handler) => {
    var collector, pipeline;
    collector = [];
    pipeline = [];
    pipeline.push(this.$as_sql());
    // pipeline.push @$as_line()
    pipeline.push(PD.$collect({collector}));
    pipeline.push(PD.$drain(function() {
      return handler(null, collector.join('\n'));
    }));
    return PD.$tee(PD.pull(...pipeline));
  };

  //-----------------------------------------------------------------------------------------------------------
  this.compile_sql = function(settings) {
    return new Promise((resolve, reject) => {
      var S, pipeline;
      validate.object(settings);
      S = settings;
      help(`reading ${rpr(S.rel_source_path)}`);
      //.........................................................................................................
      pipeline = [];
      pipeline.push(PD.read_from_file(S.source_path));
      pipeline.push(PD.$split());
      pipeline.push(this.$tee_compile_sql(S, (error, sql) => {
        help(`wrote output to ${rpr(S.target_path_sql)}`);
        return resolve(sql);
      }));
      pipeline.push(PD.$drain());
      PD.pull(...pipeline);
      //.........................................................................................................
      return null;
    });
  };

  //-----------------------------------------------------------------------------------------------------------
  _$count = function(step) {
    var nr;
    nr = 0;
    return PD.$watch((d) => {
      nr += +1;
      if ((modulo(nr, step)) === 0) {
        whisper('µ44744', nr);
      }
      return null;
    });
  };

  //-----------------------------------------------------------------------------------------------------------
  this.populate_db = function(me, sql) {
    return new Promise((resolve, reject) => {
      var line_count;
      validate.object(me);
      me.db.$.execute(sql);
      line_count = me.db.$.first_value(me.db.count_lines());
      info(`MKTS document ${rpr(me.rel_source_path)} has ${line_count} lines`);
      return resolve();
    });
  };

  //-----------------------------------------------------------------------------------------------------------
  this.cleanup = function(settings) {
    return new Promise((resolve, reject) => {
      settings.remove_tmpfile();
      return resolve();
    });
  };

  //-----------------------------------------------------------------------------------------------------------
  this.new_mirage = function(settings) {
    var R, isa_text;
    validate.true((isa_text = isa.text(settings)) || (isa.object(settings)));
    if (isa_text) {
      settings = {
        source_path: settings
      };
    }
    R = {};
    R.db = (require('./db')).new_db({
      clear: false
    });
    R.source_path = settings.source_path;
    R.rel_source_path = relpath(R.source_path);
    return R;
  };

  //-----------------------------------------------------------------------------------------------------------
  this.acquire = function(me) {
    return new Promise(async(resolve, reject) => {
      var sql;
      sql = (await this.compile_sql(me));
      await this.populate_db(me, sql);
      return resolve();
    });
  };

  //###########################################################################################################
  if (module.parent == null) {
    MIRAGE = this;
    (async function() {
      var mirage;
      //.......................................................................................................
      mirage = MIRAGE.new_mirage('./README.md');
      await MIRAGE.acquire(mirage);
      delete mirage.db;
      debug('µ69688', mirage);
      return help('ok');
    })();
  }

}).call(this);

//# sourceMappingURL=main.js.map
